apiVersion: v1
kind: Service
metadata:
  name: new-devops-next-js
  namespace: teem
  labels:
    app.kubernetes.io/name: new-devops-next-js
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: new-devops-next-js
  ports:
    - name: http
      port: 80
      targetPort: 3000
